---
phase: "27"
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - "tests/phase27/__init__.py"
  - "tests/phase27/conftest.py"
  - "tests/phase27/test_trame_postmessage.py"
  - "tests/phase27/test_filter_registry.py"
  - "tests/phase27/test_visualization_api.py"
  - "tests/phase27/test_docker_lifecycle.py"
  - "tests/phase27/test_session_isolation.py"
autonomous: true
requirements:
  - "TRAME-05.1"
  - "TRAME-05.3"
  - "TRAME-05.5"
  - "TRAME-05.6"

must_haves:
  truths:
    - "trame_server.py handles all 14 postMessage command types correctly"
    - "UUID filter registry survives create/list/delete operations"
    - "API launches Docker container, health-checks, and shuts down correctly"
    - "Concurrent sessions are isolated (no cross-contamination)"
  artifacts:
    - path: "tests/phase27/test_trame_postmessage.py"
      provides: "14 postMessage handler coverage, all switch cases"
    - path: "tests/phase27/test_filter_registry.py"
      provides: "UUID registry CRUD, restart stability"
    - path: "tests/phase27/test_visualization_api.py"
      provides: "API session lifecycle: launch, status, heartbeat, shutdown"
    - path: "tests/phase27/test_docker_lifecycle.py"
      provides: "Docker start/health/shutdown/idle-timeout paths"
    - path: "tests/phase27/test_session_isolation.py"
      provides: "Multi-session isolation"
---

<objective>
Write integration tests covering the trame backend (trame_server.py), the FastAPI visualization router, Docker session lifecycle, and session isolation. These tests validate the server-side portion of the TRAME-05 requirements.

Purpose: Validate all server-side integration points before manual feature-parity testing.
Output: Test files under tests/phase27/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@trame_server.py
@api_server/routers/visualization.py
@api_server/services/trame_session_manager.py
@tests/api_tests/conftest.py
@tests/conftest.py

## Key interfaces

From trame_server.py (postMessage switch cases, lines 614-660):
- case 'field': sets state.field
- case 'slice': sets state.slice_axis + state.slice_origin
- case 'slice_off': sets state.slice_axis = null
- case 'color_preset': sets state.color_preset
- case 'scalar_range': sets scalar_range_mode/min/max
- case 'volume_toggle': calls ctrl.on_volume_rendering_toggle
- case 'timestep': sets state.time_step_index
- case 'clip_create': calls ctrl.on_filter_clip_create
- case 'contour_create': calls ctrl.on_filter_contour_create
- case 'streamtracer_create': calls ctrl.on_filter_streamtracer_create
- case 'filter_delete': calls ctrl.on_filter_delete
- case 'filter_list': sets state.filter_list_request = true
- case 'volume_status': sets state.volume_rendering_status_request = true
- case 'screenshot': calls ctrl.on_screenshot

From trame_session_manager.py:
- launch_session(session_id, case_dir, port, job_id) -> TrameSession
- shutdown_session(session_id)
- get_session(session_id) -> Optional[TrameSession]
- update_activity(session_id)
- validate_docker_available() -> bool
- _wait_for_ready(session_id, container_id, port, timeout=60)

From visualization.py:
- POST /visualization/launch (201) -> VisualizationLaunchResponse
- GET /visualization/{session_id} (200) -> VisualizationStatusResponse
- POST /visualization/{session_id}/activity (200)
- DELETE /visualization/{session_id} (200)

Filter UUID registry (_state.filters, dict):
- Key: uuid.uuid4().hex (16-char hex string)
- Value: {"type": str, "proxy": object, "params": dict}
- Operations: on_filter_clip_create, on_filter_contour_create, on_filter_streamtracer_create, on_filter_delete, on_filter_list_request
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase27 test infrastructure</name>
  <files>
    - tests/phase27/__init__.py
    - tests/phase27/conftest.py
  </files>
  <read_first>
    tests/conftest.py
    tests/api_tests/conftest.py
  </read_first>
  <action>
Create tests/phase27/__init__.py (empty).

Create tests/phase27/conftest.py with:
1. Python path setup (add project root to sys.path before api_server imports)
2. Mock docker fixture: patch shutil.which("docker") and asyncio.create_subprocess_exec
3. Mock TrameSession model fixture: return a mock TrameSession object with fields: session_id, job_id, container_id, port, case_dir, auth_key, status, created_at, last_activity
4. Mock aiohttp fixture: patch aiohttp.ClientSession.get to return mocked 200 response
5. Mock _detect_gpu fixture: return (True, "NVIDIA") or (False, "Mesa") as needed
6. Auto-use anyio_backend = "asyncio" pytest marker

conftest.py must import from api_server.models: TrameSession (or mock if not yet updated).
</action>
  <verify>
    <automated>ls tests/phase27/ && python3 -c "import sys; sys.path.insert(0,'/Users/Zhuanz/Desktop/notion-cfd-harness'); from tests.phase27 import conftest; print('import ok')"</automated>
  </verify>
  <done>tests/phase27/ directory created with __init__.py and conftest.py, both import without errors</done>
</task>

<task type="auto">
  <name>Task 2: Write postMessage handler and filter registry tests</name>
  <files>
    - tests/phase27/test_trame_postmessage.py
    - tests/phase27/test_filter_registry.py
  </files>
  <read_first>
    trame_server.py
  </read_first>
  <action>
## test_trame_postmessage.py

Write pytest tests that parse the trame_server.py custom_script JavaScript block (lines 600-673) and verify all 14 case statements exist and are correct. Also verify the bridge window._trameBridge.postMessage("ready") call is present.

Do NOT require a running ParaView or trame server. Parse the JS string directly and assert on its content.

```python
SCRIPT_BLOCK_START = 'window._trameBridge = window._trameBridge || {};'
SCRIPT_BLOCK_END = '}, 500);'

def get_custom_script() -> str:
    """Extract the injected JS script block from trame_server.py source."""
    with open(PROJECT_ROOT / "trame_server.py") as f:
        content = f.read()
    # find the script block between SCRIPT_BLOCK_START and SCRIPT_BLOCK_END
    ...
```

Tests:
1. test_all_14_message_cases_exist: grep the script block for each case: 'field', 'slice', 'slice_off', 'color_preset', 'scalar_range', 'volume_toggle', 'timestep', 'clip_create', 'contour_create', 'streamtracer_create', 'filter_delete', 'filter_list', 'volume_status', 'screenshot'
2. test_ready_message_broadcast: assert 'window._trameBridge.postMessage({ type: \'ready\' }, \'*\');' in script
3. test_camera_polling_interval: assert 'setInterval' and 'camera_poll_trigger' in script, polling at 500ms
4. test_volume_toggle_calls_ctrl: assert 'ctrl.on_volume_rendering_toggle' in script for volume_toggle case
5. test_filter_delete_calls_ctrl: assert 'ctrl.on_filter_delete' in script for filter_delete case
6. test_screenshot_calls_ctrl: assert 'ctrl.on_screenshot' in script for screenshot case

## test_filter_registry.py

Write tests for the UUID filter registry by directly exercising trame_server.py module-level functions (parse the Python source to extract _build_filter_params, _get_filter_list, on_filter_clip_create, etc.).

Tests using unittest.mock.patch on simple module:
1. test_filter_clip_creates_uuid_key: mock simple.GetActiveSource, simple.Clip, simple.Render, _ctrl.view_update, _state.filters. Call on_filter_clip_create(inside_out=False, scalar_value=0.0). Assert filter_uuid is a 16-char hex string and is added to _state.filters dict.
2. test_filter_contour_creates_uuid_key: same for on_filter_contour_create(isovalues=[0.5])
3. test_filter_streamtracer_creates_uuid_key: same for on_filter_streamtracer_create(integration_direction="FORWARD", max_steps=1000)
4. test_filter_delete_removes_uuid_key: pre-populate _state.filters with a fake entry, call on_filter_delete with that UUID, assert it's removed from _state.filters
5. test_filter_list_builds_correct_structure: add 3 filters (clip, contour, streamtracer), call _get_filter_list(), assert result["filters"] has 3 entries each with "id", "type", "parameters" keys
6. test_volume_rendering_status_detects_mesa: mock _detect_gpu to return (False, "Mesa"), call on_volume_rendering_status_request, assert _state.volume_rendering_status["gpu_vendor"] == "Mesa" and ["gpu_available"] == False
7. test_volume_rendering_status_warns_over_2m_cells: mock data_info.GetNumberOfCells to return 3000000, call on_volume_rendering_status_request, assert cell_count_warning == True
</action>
  <verify>
    <automated>cd /Users/Zhuanz/Desktop/notion-cfd-harness && python3 -m pytest tests/phase27/test_trame_postmessage.py tests/phase27/test_filter_registry.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>test_trame_postmessage.py passes (all 14 cases verified in JS script), test_filter_registry.py passes (UUID ops, GPU/Mesa detection, 2M cell warning)</done>
</task>

<task type="auto">
  <name>Task 3: Write API endpoint and Docker lifecycle tests</name>
  <files>
    - tests/phase27/test_visualization_api.py
    - tests/phase27/test_docker_lifecycle.py
  </files>
  <read_first>
    api_server/routers/visualization.py
    api_server/services/trame_session_manager.py
    tests/api_tests/conftest.py
  </read_first>
  <action>
## test_visualization_api.py

Write pytest-asyncio tests using the FastAPI TestClient pattern from api_tests/conftest.py.

Use mock.patch on:
- get_trame_session_manager() to return a mock manager
- TrameSession to return mock objects

Tests:
1. test_launch_visualization_session_returns_201: POST /visualization/launch with {job_id: "J1", case_dir: "/tmp/testcase"} (mock validate), assert 201, response has session_id, session_url (http://localhost:{port}), auth_key, port
2. test_launch_validates_case_dir_absolute: POST with case_dir="relative/path" (not absolute), assert 400 with detail containing "absolute"
3. test_launch_validates_case_dir_exists: POST with case_dir="/nonexistent/path", assert 400
4. test_get_session_returns_200: GET /visualization/PVW-12345678 after mock setup, assert 200, body has session_id, status, port
5. test_get_session_returns_404: GET /visualization/NONEXISTENT, assert 404
6. test_update_activity_returns_200: POST /visualization/PVW-12345678/activity, assert 200
7. test_delete_session_returns_200: DELETE /visualization/PVW-12345678, assert 200, body has "stopped"
8. test_session_url_uses_http_not_ws: launch response session_url starts with "http://localhost:" (not "ws://")

## test_docker_lifecycle.py

Test TrameSessionManager Docker lifecycle using mock subprocesses.

Use mock.patch for:
- shutil.which("docker") -> "/usr/bin/docker"
- asyncio.create_subprocess_exec for: docker run, docker inspect, docker kill
- aiohttp.ClientSession.get for HTTP health check
- _detect_gpu inside trame_server module

Tests:
1. test_launch_session_runs_docker_run_command: call launch_session, assert docker run was called with "--name", "trame-{session_id}", "-p {port}:9000", "pvpython", "/trame_server.py"
2. test_launch_session_mounts_case_dir_readonly: docker run called with "-v {case_path}:/data:ro"
3. test_launch_session_waits_for_http_200: _wait_for_ready polls and returns when HTTP 200 received
4. test_launch_session_raises_if_container_exits_early: docker inspect returns "false" before timeout, assert TrameSessionError with "Container exited during startup"
5. test_launch_session_raises_on_docker_unavailable: validate_docker_available returns False, assert TrameSessionError
6. test_shutdown_session_kills_container: call shutdown_session, assert docker kill called with container_name
7. test_idle_timeout_shuts_down: set session.last_activity to 31 minutes ago, call _shutdown_idle_sessions, assert docker kill was called
8. test_multiple_sessions_get_different_ports: launch two sessions (no port specified), assert port1 != port2
</action>
  <verify>
    <automated>cd /Users/Zhuanz/Desktop/notion-cfd-harness && python3 -m pytest tests/phase27/test_visualization_api.py tests/phase27/test_docker_lifecycle.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>test_visualization_api.py: 8 tests pass; test_docker_lifecycle.py: 8 tests pass; session isolation (test_multiple_sessions_get_different_ports) passes</done>
</task>

<task type="auto">
  <name>Task 4: Write session isolation test</name>
  <files>
    - tests/phase27/test_session_isolation.py
  </files>
  <read_first>
    api_server/services/trame_session_manager.py
  </read_first>
  <action>
Write test_session_isolation.py to verify that two concurrent trame sessions are fully isolated.

The key isolation mechanisms are:
1. Each session gets its own container (docker run --name trame-{session_id})
2. Each session has its own port (auto-allocated from range 8081-8090)
3. Each session has its own auth_key
4. _state.filters in trame_server.py is per-server-instance (get_server(name=session_id))

Tests:
1. test_two_sessions_have_separate_containers: launch two sessions, assert container_name_1 != container_name_2
2. test_two_sessions_have_separate_ports: launch two sessions, assert port_1 != port_2
3. test_two_sessions_have_separate_auth_keys: launch two sessions, assert auth_key_1 != auth_key_2
4. test_get_session_returns_correct_session: launch two sessions, get_session(id1) returns session1 (not session2), get_session(id2) returns session2
5. test_update_activity_only_affects_target_session: session1 last_activity at T0, session2 last_activity at T0, update_activity(session1), assert session1.last_activity > T0 and session2.last_activity == T0
6. test_shutdown_one_session_preserves_other: launch two sessions, shutdown_session(id1), assert session2 still in _sessions, session1 not in _sessions

Use the same mock approach as test_docker_lifecycle.py.
</action>
  <verify>
    <automated>cd /Users/Zhuanz/Desktop/notion-cfd-harness && python3 -m pytest tests/phase27/test_session_isolation.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>test_session_isolation.py: 6 tests pass, confirming concurrent sessions are isolated</done>
</task>

</tasks>

<verification>
All 27-01 tests pass: 8 (API) + 8 (Docker lifecycle) + 6 (postMessage/filter registry) + 6 (session isolation) = 28 tests.
Run: python3 -m pytest tests/phase27/ -v --tb=short 2>&1 | tail -40
</verification>

<success_criteria>
- tests/phase27/ contains 6 test files, all pass
- All 14 postMessage case types verified in the JS script block
- UUID filter registry tests cover create/list/delete and survive mock server restart
- API endpoint tests cover launch (201), get (200/404), activity (200), delete (200)
- Docker lifecycle tests cover: docker run args, port allocation, health check polling, container-exit error, idle timeout, separate container per session
- Session isolation tests: 2 sessions get different containers/ports/auth_keys, shutdown is targeted
- Screenshot debounce pipeline not tested here (Plan 27-02 covers the frontend debounce)
</success_criteria>

<output>
After completion, create `.planning/phases/27-integration-feature-parity/27-01-SUMMARY.md`
</output>
